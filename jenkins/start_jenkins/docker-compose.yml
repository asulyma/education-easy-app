version: '3'
services:
  jenkins:
    container_name: jenkins
    build: .
    user: root 
    ports:
      - "8080:8080"
    volumes:
      - "/home/aleksandr/jenkins_volume:/var/jenkins_home"
      - //var/run/docker.sock:/var/run/docker.sock
    networks:
      - net

networks:
  net:
 
